# Seecript v0.1 后端依赖
# 部署到生产时建议 pip install -r requirements.txt --no-cache-dir
# 本地开发额外装 pytest（见 requirements-dev.txt）

# Web framework
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30,<0.35
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0
python-multipart>=0.0.9,<1.0  # required for FastAPI UploadFile

# HTTP client (用于调 DeepSeek + 阿里 DashScope)
httpx>=0.27,<0.30

# 生产 WSGI/ASGI 进程管理（systemd 内）
gunicorn>=22.0,<25.0
