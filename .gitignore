# Runtime
.server.pid
logs/
server/var/
server/server/
server/.dev.log
server/.dev.err
.dev-server.log
.dev-server.err
.dev-server.out

# Local scratch — agent progress notes & one-off curl payloads
.scratch/
server/.scratch/

# Secrets — NEVER commit these
.env
server/.env
server/.env.local
server/.env.server-prod
*.bak

# Python
server/venv/
server/.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

# Node — web/ 与 remotion/
node_modules/
web/dist/
web/.vite/
remotion/dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Transient git scratch files (commit message scratch via PowerShell heredoc workaround)
.git_commit_msg.tmp
*.tmp

# Third-party agent skill library — installed globally via `npx skills@latest add ...`,
# do not vendor in this repo. See CLAUDE.md "Agent skills" section for install steps.
mattpocock-skills-zh-CN/

# Claude Code machine-local settings (shared settings.json IS tracked)
.claude/settings.local.json

# Local-only docs (not for github)
LOCAL_DEPLOY.md

# Transient tsc output dumps
web/tsc_out.txt
